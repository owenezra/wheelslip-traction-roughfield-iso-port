# Task

Replace this with the instructions the agent should follow.

Write the final answer to `/tmp/output/answer.txt`.

If the task needs Python libraries or command-line tools, name them directly in
this prompt. Do not point the agent at internal build/source metadata for
package availability. Taiga export automatically adds runtime resource and
long-running-session guidance when applicable.
