schema_version = "1.1"

[task]
name = "labelbox/mujoco-replace-me"
description = "Train or author a policy for a MuJoCo robotics simulation."

[environment]
required_resources = "16vcpu+128gib"
storage_mb = 50000
allow_internet = true

[agent]
timeout_sec = 7200

[verifier]
timeout_sec = 1200
env = []

[difficulty]
task_type = "mujoco"
domain = "policy_training_improvement"
reward_type = "multi_deterministic_rubrics"
[ground_truth]
render_command = "bash solution/render.sh"
render_outputs = [
  { path = "/tmp/output/rendering.mp4", required = true, description = "Reviewer video of the oracle policy rollout" },
]

[[outputs]]
path = "/tmp/output/policy.py"
required = true
description = "Python policy module exposing act(obs) or Policy.act(obs)"

[[outputs]]
path = "/tmp/output/README.md"
required = false
description = "Optional notes about the solution approach"
